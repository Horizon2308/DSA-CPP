#include<bits/stdc++.h>
//push_back
//size()
//back()
//vector<int>::iterator it
//begin()=> iterator tro toi phan tu dau tien
//end()=> iterator tro toi phan tu sau phan tu cuoi
//r.begin()=>iterator tro toi phan tu cuoi
//r.end()=> iterator tro toi truoc phan tu dau tien
//insert(pos,value)
//insert(pos,n,value) => chen n phan tu vao pos
//clear() => xoa toan bo vector
//.swap(vt2) => hoan doi vector
//front() => vi tri dau tien cua vector trong mang
//back() => vi tri cuoi cung cua vector trong mang
//resize(n)=>thay doi kich thuoc cua vector
using namespace std;
/*int main()
{
	/*vector<int>v;
	v.push_back(10);
	v.push_back(20);
	v.push_back(30);
	cout <<v.size()<<endl;
	cout <<v[0]<<endl;
	cout << v.back();
	string s;
	getline(cin,s);
	cout <<s<<endl;
	stringstream ss(s);
	string tmp;
	vector<string>v;
	while(ss>>tmp)
	{
		v.push_back(tmp);
	}
	for(int i=0; i<v.size(); i++)
	{
		cout <<v[i]<<endl;
	}
	return 0;
}*/
int main()
{
	int n;cin>>n;
	int a[n];
	vector<int>v;
	for(int i=0; i<n; i++)
	{
		cin>>a[i];
		v.push_back();
	}
	
}